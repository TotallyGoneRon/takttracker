---
phase: 07-walk-history-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/companies/page.tsx
  - src/app/schedule/[planId]/scorecard/page.tsx
  - src/lib/statusColors.ts
  - src/app/schedule/[planId]/page.tsx
  - src/app/schedule/[planId]/site-walk/_components/types.ts
  - src/app/schedule/[planId]/site-walk/_components/EntryCard.tsx
  - src/app/schedule/[planId]/site-walk/_components/WalkSummary.tsx
  - src/app/schedule/[planId]/site-walk/_components/StatusSelector.tsx
  - src/app/schedule/[planId]/site-walk/_components/ZoneSelector.tsx
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

must_haves:
  truths:
    - "Companies page loads data for the most recent plan, not hardcoded plan 1"
    - "Scorecard 'View downstream impact' is a clickable link that navigates to the schedule timeline"
    - "No local STATUS_COLORS or STATUS_DOT_COLORS definitions remain in schedule/[planId]/page.tsx"
    - "Site-walk types.ts derives its button colors and zone colors from the shared statusColors module"
    - "Scorecard inline dot color ternary replaced with shared STATUS_COLORS import"
  artifacts:
    - path: "src/lib/statusColors.ts"
      provides: "Extended shared module with hover property for button use cases"
      contains: "hover"
    - path: "src/app/companies/page.tsx"
      provides: "Dynamic plan selection using /api/plans"
      contains: "/api/plans"
    - path: "src/app/schedule/[planId]/scorecard/page.tsx"
      provides: "Working downstream impact link"
      contains: "Link"
  key_links:
    - from: "src/app/schedule/[planId]/page.tsx"
      to: "src/lib/statusColors.ts"
      via: "import { STATUS_COLORS }"
      pattern: "import.*STATUS_COLORS.*from.*statusColors"
    - from: "src/app/schedule/[planId]/site-walk/_components/types.ts"
      to: "src/lib/statusColors.ts"
      via: "import and derive"
      pattern: "import.*from.*statusColors"
---

<objective>
Fix three existing bugs: companies page hardcoded plan ID, scorecard dead link, and local color constant cleanup.

Purpose: Resolve known bugs before v1.1 ships -- companies page shows wrong data for users with multiple plans, scorecard has a non-functional link, and color constants are duplicated across files making maintenance fragile.
Output: Three bug fixes across 9 files, all using the shared statusColors.ts module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-walk-history-bug-fixes/07-CONTEXT.md
@.planning/phases/07-walk-history-bug-fixes/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/statusColors.ts (current):
```typescript
export const STATUS_COLORS = {
  completed: { bg: 'bg-green-400', text: 'text-green-800', light: 'bg-green-100', border: 'border-green-500', dot: 'bg-green-500', label: 'Complete' },
  delayed: { bg: 'bg-red-400', text: 'text-red-800', light: 'bg-red-100', border: 'border-red-500', dot: 'bg-red-500', label: 'Delayed' },
  at_risk: { bg: 'bg-yellow-400', text: 'text-yellow-800', light: 'bg-yellow-100', border: 'border-yellow-500', dot: 'bg-yellow-500', label: 'At Risk' },
  recovered: { bg: 'bg-blue-400', text: 'text-blue-800', light: 'bg-blue-100', border: 'border-blue-500', dot: 'bg-blue-500', label: 'Recovered' },
  in_progress: { bg: 'bg-indigo-300', text: 'text-indigo-800', light: 'bg-indigo-100', border: 'border-indigo-500', dot: 'bg-indigo-500', label: 'In Progress' },
  not_started: { bg: 'bg-gray-200', text: 'text-gray-700', light: 'bg-gray-100', border: 'border-gray-400', dot: 'bg-gray-400', label: 'Not Started' },
  on_track: { bg: 'bg-green-300', text: 'text-green-800', light: 'bg-green-100', border: 'border-green-400', dot: 'bg-green-400', label: 'On Track' },
  blocked: { bg: 'bg-red-200', text: 'text-red-800', light: 'bg-red-200', border: 'border-red-700', dot: 'bg-red-700', label: 'Blocked' },
} as const;
export type StatusKey = keyof typeof STATUS_COLORS;
```

From src/app/schedule/[planId]/site-walk/_components/types.ts (STATUS_COLORS - button format):
```typescript
export const STATUS_COLORS: Record<string, { bg: string; hover: string; text: string; label: string }> = {
  completed: { bg: 'bg-emerald-600', hover: 'hover:bg-emerald-700', text: 'text-white', label: 'Completed' },
  on_track: { bg: 'bg-green-500', hover: 'hover:bg-green-600', text: 'text-white', label: 'On Track' },
  delayed: { bg: 'bg-red-500', hover: 'hover:bg-red-600', text: 'text-white', label: 'Delayed' },
  recovered: { bg: 'bg-blue-500', hover: 'hover:bg-blue-600', text: 'text-white', label: 'Recovered' },
};
```

From src/app/schedule/[planId]/site-walk/_components/types.ts (ZONE_STATUS_COLORS):
```typescript
export const ZONE_STATUS_COLORS: Record<string, string> = {
  completed: 'bg-green-400 border-green-500',
  delayed: 'bg-red-400 border-red-500',
  on_track: 'bg-green-300 border-green-400',
  recovered: 'bg-blue-400 border-blue-500',
  not_started: 'bg-gray-200 border-gray-300',
  in_progress: 'bg-indigo-300 border-indigo-400',
  at_risk: 'bg-yellow-300 border-yellow-400',
};
```

From src/app/schedule/[planId]/page.tsx (local definitions to remove, lines 49-63):
```typescript
const STATUS_COLORS: Record<string, string> = {
  not_started: 'bg-gray-200 text-gray-700',
  in_progress: 'bg-indigo-100 text-indigo-700',
  completed: 'bg-green-100 text-green-700',
  delayed: 'bg-red-100 text-red-700',
  blocked: 'bg-red-200 text-red-800',
};
const STATUS_DOT_COLORS: Record<string, string> = {
  not_started: 'bg-gray-400',
  in_progress: 'bg-indigo-500',
  completed: 'bg-green-500',
  delayed: 'bg-red-500',
  blocked: 'bg-red-700',
};
```

Usage in schedule/[planId]/page.tsx:
- Line 387: `STATUS_DOT_COLORS[task.status]` -> replace with `STATUS_COLORS[task.status]?.dot`
- Line 399: `STATUS_COLORS[task.status]` -> replace with combined `STATUS_COLORS[task.status]?.light + ' ' + STATUS_COLORS[task.status]?.text`

From src/app/companies/page.tsx (lines 53-58 to fix):
```typescript
const { data: planData } = useSWR<{ plan: { id: number; project_id: number } }>(
  '/api/plans/1?limit=1'
);
const projectId = planData?.plan?.project_id ?? null;
const planId = planData?.plan?.id ?? null;
```

From src/app/schedule/[planId]/scorecard/page.tsx (lines 470-472 to fix):
```tsx
<div className="text-xs text-blue-600 cursor-default opacity-50 mt-1">
  View downstream impact
</div>
```
Note: `planId` available via `const params = useParams()` and `params.planId`. Link already imported.

From scorecard (lines 441-446 inline dot colors to fix):
```tsx
<div className={`w-2 h-2 rounded-full flex-shrink-0 mt-1.5 ${
  task.status === 'completed' ? 'bg-green-500' :
  task.status === 'delayed' ? 'bg-red-500' :
  task.status === 'in_progress' ? 'bg-indigo-500' :
  'bg-gray-400'
}`} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared statusColors and fix companies + scorecard bugs</name>
  <files>
    src/lib/statusColors.ts,
    src/app/companies/page.tsx,
    src/app/schedule/[planId]/scorecard/page.tsx
  </files>
  <read_first>
    src/lib/statusColors.ts,
    src/app/companies/page.tsx,
    src/app/schedule/[planId]/scorecard/page.tsx
  </read_first>
  <action>
    **1. Extend statusColors.ts (per D-08):**
    Add a `hover` property to each status entry in the STATUS_COLORS object. Use these exact values:
    - completed: `'hover:bg-green-500'`
    - delayed: `'hover:bg-red-500'`
    - at_risk: `'hover:bg-yellow-500'`
    - recovered: `'hover:bg-blue-500'`
    - in_progress: `'hover:bg-indigo-400'`
    - not_started: `'hover:bg-gray-300'`
    - on_track: `'hover:bg-green-400'`
    - blocked: `'hover:bg-red-300'`

    Also add a helper to derive zone status colors:
    ```typescript
    export const ZONE_STATUS_COLORS: Record<string, string> = Object.fromEntries(
      Object.entries(STATUS_COLORS).map(([key, val]) => [key, `${val.bg} ${val.border}`])
    );
    ```

    And add a WALK_STATUSES array for the 4 statuses used by StatusSelector buttons:
    ```typescript
    export const WALK_STATUSES = ['completed', 'on_track', 'delayed', 'recovered'] as const;
    ```

    And add walk-specific button colors (these use different shades than the generic STATUS_COLORS for large interactive buttons):
    ```typescript
    export const WALK_BUTTON_COLORS: Record<string, { bg: string; hover: string; text: string; label: string }> = {
      completed: { bg: 'bg-emerald-600', hover: 'hover:bg-emerald-700', text: 'text-white', label: 'Completed' },
      on_track: { bg: 'bg-green-500', hover: 'hover:bg-green-600', text: 'text-white', label: 'On Track' },
      delayed: { bg: 'bg-red-500', hover: 'hover:bg-red-600', text: 'text-white', label: 'Delayed' },
      recovered: { bg: 'bg-blue-500', hover: 'hover:bg-blue-600', text: 'text-white', label: 'Recovered' },
    };
    ```

    **2. Fix companies page (per D-05, D-06):**
    In `src/app/companies/page.tsx`, replace lines 53-58. Change the SWR call from fetching a single plan to fetching the plans list, then pick the most recent:
    ```typescript
    const { data: plans } = useSWR<Array<{ id: number; project_id: number }>>('/api/plans');
    const latestPlan = plans?.sort((a, b) => b.id - a.id)[0] ?? null;
    const projectId = latestPlan?.project_id ?? null;
    const planId = latestPlan?.id ?? null;
    ```
    Also update `const loading = !planData;` to `const loading = !plans;` (line ~67).

    **3. Fix scorecard dead link (per D-07):**
    In `src/app/schedule/[planId]/scorecard/page.tsx`:
    - At lines 470-472, replace the non-interactive `<div>` with a Next.js `<Link>`:
      ```tsx
      <Link
        href={`/schedule/${params.planId}`}
        className="text-xs text-blue-600 hover:text-blue-800 mt-1 inline-block"
      >
        View downstream impact
      </Link>
      ```
      Note: `Link` is already imported and `params` is already available from `useParams()` at the top of the component. Verify `params.planId` is used (not a different variable name) by checking the component.

    - At lines 441-446, replace the inline ternary dot color with shared STATUS_COLORS import. Add `import { STATUS_COLORS } from '@/lib/statusColors';` to the imports. Then replace:
      ```tsx
      <div className={`w-2 h-2 rounded-full flex-shrink-0 mt-1.5 ${
        STATUS_COLORS[task.status as keyof typeof STATUS_COLORS]?.dot || 'bg-gray-400'
      }`} />
      ```
  </action>
  <verify>
    <automated>cd /home/ronpi/projects/zach && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/companies/page.tsx` contains `'/api/plans'` (not `'/api/plans/1'`)
    - `src/app/companies/page.tsx` contains `const loading = !plans`
    - `src/app/schedule/[planId]/scorecard/page.tsx` contains `href={` on the "View downstream impact" line
    - `src/app/schedule/[planId]/scorecard/page.tsx` contains `import { STATUS_COLORS } from '@/lib/statusColors'`
    - `src/app/schedule/[planId]/scorecard/page.tsx` does NOT contain `cursor-default opacity-50` (the old dead link styling)
    - `src/lib/statusColors.ts` contains `hover:` properties on each status entry
    - `src/lib/statusColors.ts` contains `ZONE_STATUS_COLORS`
    - `src/lib/statusColors.ts` contains `WALK_BUTTON_COLORS`
    - `src/lib/statusColors.ts` contains `WALK_STATUSES`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Companies page fetches the latest plan dynamically, scorecard link navigates to the schedule timeline, and statusColors.ts is extended with hover, zone colors, walk button colors, and walk statuses exports</done>
</task>

<task type="auto">
  <name>Task 2: Replace all local color constants with shared statusColors imports</name>
  <files>
    src/app/schedule/[planId]/page.tsx,
    src/app/schedule/[planId]/site-walk/_components/types.ts,
    src/app/schedule/[planId]/site-walk/_components/EntryCard.tsx,
    src/app/schedule/[planId]/site-walk/_components/WalkSummary.tsx,
    src/app/schedule/[planId]/site-walk/_components/StatusSelector.tsx,
    src/app/schedule/[planId]/site-walk/_components/ZoneSelector.tsx
  </files>
  <read_first>
    src/app/schedule/[planId]/page.tsx,
    src/app/schedule/[planId]/site-walk/_components/types.ts,
    src/app/schedule/[planId]/site-walk/_components/EntryCard.tsx,
    src/app/schedule/[planId]/site-walk/_components/WalkSummary.tsx,
    src/app/schedule/[planId]/site-walk/_components/StatusSelector.tsx,
    src/app/schedule/[planId]/site-walk/_components/ZoneSelector.tsx,
    src/lib/statusColors.ts
  </read_first>
  <action>
    **1. schedule/[planId]/page.tsx (per D-08):**
    - Delete the local `STATUS_COLORS` definition (lines 49-55) and `STATUS_DOT_COLORS` definition (lines 57-63)
    - Add import: `import { STATUS_COLORS } from '@/lib/statusColors';`
    - At line 387 (dot usage), replace `STATUS_DOT_COLORS[task.status] || 'bg-gray-400'` with `STATUS_COLORS[task.status as keyof typeof STATUS_COLORS]?.dot || 'bg-gray-400'`
    - At line 399 (badge usage), replace `STATUS_COLORS[task.status] || ''` with `${STATUS_COLORS[task.status as keyof typeof STATUS_COLORS]?.light || ''} ${STATUS_COLORS[task.status as keyof typeof STATUS_COLORS]?.text || ''}`

    **2. site-walk types.ts (per D-08):**
    - Remove the local `STATUS_COLORS` export (lines 83-88) entirely
    - Remove the local `ZONE_STATUS_COLORS` export (lines 101-109) entirely
    - Add import at top: `import { WALK_BUTTON_COLORS, ZONE_STATUS_COLORS } from '@/lib/statusColors';`
    - Re-export for backward compatibility so downstream files don't all need to change their imports:
      ```typescript
      export { WALK_BUTTON_COLORS as STATUS_COLORS, ZONE_STATUS_COLORS } from '@/lib/statusColors';
      ```
      This keeps the import name `STATUS_COLORS` in EntryCard, WalkSummary, and StatusSelector unchanged while sourcing from the shared module.

    IMPORTANT: The StatusSelector iterates `Object.entries(STATUS_COLORS)` to render buttons. Since types.ts re-exports `WALK_BUTTON_COLORS as STATUS_COLORS`, StatusSelector will only show the 4 walk statuses (completed, on_track, delayed, recovered) -- this is correct and matches current behavior. Do NOT import the full 8-status STATUS_COLORS from statusColors.ts into StatusSelector.

    **3. EntryCard.tsx, WalkSummary.tsx, StatusSelector.tsx, ZoneSelector.tsx:**
    These files import from `./types` which re-exports the colors. No changes needed unless TypeScript errors arise from the type change. If `StatusSelector.tsx` iterates `Object.entries(STATUS_COLORS)`, confirm it still works with the re-exported `WALK_BUTTON_COLORS` shape (it has the same `{ bg, hover, text, label }` shape so it will).

    For `ZoneSelector.tsx`, it imports `ZONE_STATUS_COLORS` from `./types`. Since types.ts now re-exports `ZONE_STATUS_COLORS` from the shared module, this should work without changes. Verify the shape is compatible -- both are `Record<string, string>` with `'bg-X border-X'` format.
  </action>
  <verify>
    <automated>cd /home/ronpi/projects/zach && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/schedule/[planId]/page.tsx` does NOT contain `const STATUS_COLORS` or `const STATUS_DOT_COLORS`
    - `src/app/schedule/[planId]/page.tsx` contains `import { STATUS_COLORS } from '@/lib/statusColors'`
    - `src/app/schedule/[planId]/site-walk/_components/types.ts` does NOT contain `bg-emerald-600` (the old local STATUS_COLORS definition)
    - `src/app/schedule/[planId]/site-walk/_components/types.ts` does NOT contain `'bg-green-400 border-green-500'` (the old local ZONE_STATUS_COLORS)
    - `src/app/schedule/[planId]/site-walk/_components/types.ts` contains `from '@/lib/statusColors'`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>All local status color definitions removed; every page imports from the shared statusColors.ts module. The site-walk button colors and zone colors are derived from the centralized module via re-exports in types.ts.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "const STATUS_COLORS" src/app/` returns only re-exports, no local definitions
3. `grep -r "STATUS_DOT_COLORS" src/` returns zero matches
4. `grep "/api/plans/1" src/app/companies/` returns zero matches
5. `grep "cursor-default opacity-50" src/app/schedule/` returns zero matches
</verification>

<success_criteria>
- Companies page loads data using the most recent plan from /api/plans (not hardcoded plan 1)
- Scorecard "View downstream impact" is a working Next.js Link to the schedule timeline
- All status color usage across the app derives from src/lib/statusColors.ts
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-walk-history-bug-fixes/07-01-SUMMARY.md`
</output>
